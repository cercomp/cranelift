<h1>Recuperar Senha</h1>

<%= form_tag password_resets_path, :method => :post, :class => 'form-inline' do %>
  <%= text_field_tag :email, params[:email], :placeholder => 'Email' %>
  <%= submit_tag "Recuperar", :class => 'btn' %>
<% end %>
